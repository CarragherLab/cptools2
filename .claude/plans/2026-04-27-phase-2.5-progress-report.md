# Progress Report: Phase 2.5 Intra-Plate Batching

Generated: 2026-04-27

## Summary

- **Phase**: 2.5 — Intra-Plate Batching and Fan-Out
- **Status**: implemented locally, Eddie validation pending
- **Goal**: restore cptools2's many-jobs-per-plate capability in the Nextflow revamp
- **Core outcome**: Nextflow now owns the intra-plate fan-out while cptools2/parserix provides domain parsing inside Nextflow-called helper processes

## Completed Work

| Area | Status | Details |
|------|--------|---------|
| Chunk contract | Complete | Locked image-set chunking as the unit of work, default `chunk_size=96`, with `plate`, `well`, `site`, `channel`, `image_path`, `image_set_id`, and `chunk_id` preserved. |
| Parserix-backed indexing | Complete locally | Added `cptools2/nextflow_chunking.py` with `index` and `chunk` commands for Nextflow processes. |
| Channel validation | Complete locally | Missing required channels now fail before compute starts. |
| Batch-specific params | Complete locally | `cptools2 pipeline --dry-run` now writes `params.batch_N.json` files with active `plates` lists. |
| Nextflow fan-out | Complete at graph level | Added `BUILD_IMAGESET_INDEX` and `CHUNK_IMAGESETS` modules, then wired them into `nextflow/main.nf`. |
| Chunked illumination apply | Implemented | `ILLUM_APPLY` now consumes chunk manifests and creates a chunk image directory before CellProfiler execution. |
| AI segmentation rule | Implemented | AI feature extraction rejects explicit non-Cellpose segmentation; pure CellProfiler workflows can still avoid Cellpose. |
| Cellpose stage | Implemented locally | `CELLPOSE_SEGMENT` now filters the chunk manifest to the configured Cellpose channel, runs Cellpose with GPU, writes masks, and emits centroid location CSVs. |
| GPU throttling | Implemented | Eddie GPU-labelled processes now use `maxForks = 4` with `-q gpu -l gpu=1`. |

## Verification

| Check | Result |
|-------|--------|
| `pytest tests\test_nextflow_chunking.py tests\test_cli.py::TestCmdPipelineDryRun tests\test_parse_yaml.py tests\test_nextflow_architecture_smoke.py -q --basetemp=.tmp\pytest` | Passed, `40 passed` |
| `pytest -q --basetemp=.tmp\pytest` | Passed, `168 passed` after Cellpose parameter mapping and stage wiring |
| `nextflow config nextflow/main.nf -profile test` via `nextflow/nextflow:24.10.4` Docker image | Passed |
| `nextflow config nextflow/main.nf -profile eddie` via `nextflow/nextflow:24.10.4` Docker image | Passed |
| `nextflow run nextflow/main.nf -profile test -preview` via Docker | Passed; preview shows `BUILD_IMAGESET_INDEX`, `CHUNK_IMAGESETS`, `ILLUM_CALCULATE`, `ILLUM_APPLY`, `CELLPOSE_SEGMENT`, and `FEATURE_EXTRACT` |

## Issues and Challenges

| Issue | Impact | Outcome |
|-------|--------|---------|
| Windows pytest temp permissions blocked initial test setup | Tests could not start under the default temp path | Overcome by running pytest with `--basetemp=.tmp\pytest` and escalated permission. |
| Docker API access was denied inside the sandbox | Nextflow config smoke could not run initially | Overcome with approved Docker escalation. |
| Nextflow preview generated output files under `nextflow/tests/nf-test-data/output` | Left untracked generated artifacts in the worktree | Cleanup approval was rejected by the environment, so files were left untracked and excluded from the commit. |
| Final Cellpose command needed wiring | AI path initially had a structural scaffold only | Overcome by wiring `python -m cellpose` over chunk-selected images and writing centroid CSV outputs. |
| Eddie subset validation has not run with this new graph | Phase 2.5 is not operationally proven on Eddie yet | Kept Loop 280 pending. |

## Current Blockers

1. **Eddie deployment of new code**: the updated Python and Nextflow files need to be installed/copied into the shared Eddie project path before Loop 280.
2. **Eddie Cellpose runtime validation**: the Cellpose command is wired locally, but has not yet run on an Eddie GPU chunk.
3. **Generated artifact cleanup**: local generated folders `nextflow/tests/`, `.tmp/`, and `pytest-cache-files-*` should be removed when approval allows.

## Next Steps

1. Deploy the updated repo to `/exports/cmvm/eddie/smgphs/groups/ChandranLabs/cptools2`.
2. Run an Eddie subset validation with `max_chunks=1` or `max_chunks=2`.
3. Confirm multiple SGE jobs are submitted for one staged plate.
4. Validate Cellpose masks and centroid CSVs on an Eddie GPU node.
5. Run the representative Sarah-screen plate after subset validation passes.
