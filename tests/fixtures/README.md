# Minimal test fixtures for unit tests

This directory contains small, representative test data used by unit tests.
Do NOT commit large production datasets here. For full datasets, place them in the
`data/` directory outside of version control and update tests/configs to point to them.

If you need the full datasets, request them from the data owners or follow the project
instructions to download from the institutional storage.
