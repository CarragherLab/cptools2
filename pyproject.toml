[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88
select = ["E", "F", "W", "C90", "I"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cptools2"
version = "0.0.0"
description = "Utilities to run CellProfiler workflows on clusters"
readme = "cptools2/README.md"
requires-python = ">=3.8"
authors = [ { name = "CarragherLab" } ]
license = { text = "MIT" }
dependencies = [
    "pandas>=1.3",
    "pyyaml>=5.4",
]

[project.scripts]
cptools2 = "cptools2.__main__:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov",
    "black>=23.0",
    "isort>=5.0",
    "ruff>=0.0.280",
    "pre-commit",
]

